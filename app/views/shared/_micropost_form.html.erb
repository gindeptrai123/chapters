<%= form_for(@micropost) do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t("micropost.placeholder") %>
  </div>
  <%= f.submit t("micropost.submit"), class: "btn btn-primary" %>
<% end %>
